# SDD ledger — plan: docs/superpowers/plans/2026-09-24-sichere-erkennung-ui.md

Baseline: `./gradlew testDebugUnitTest --rerun-tasks` → BUILD SUCCESSFUL (2026-09-24); existing TensorFlow manifest namespace warnings and one CameraX `setTargetResolution` deprecation observed.
Pre-flight: Task 1 produces only conservative `VerdictDecision`; Task 2 consumes that decision unchanged through `ResultFormatter.format` — consistent with the spec.
Pre-flight: Task 3 consumes only existing view/resource identifiers and is independent of the Task-1 Policy interface.
Task 1: Ruling: `task-start` lives under `skills/executing-plans/scripts`, while only `sdd-workspace` lives under `skills/subagent-driven-development/scripts` — use the actual v6.4.1 script locations — cost if wrong: workflow bookkeeping, not app behavior.
Task 1: Ruling: `ResultFormatterTest` consumes Task-1 policy output and encoded the superseded SAFE contract — update its consumer expectations before the first commit so the required full suite can become green — cost if wrong: the conservative policy could not ship as a coherent tested contract.
Task 1: complete (commits b02c006..cc9ed93, tests: ./gradlew testDebugUnitTest --rerun-tasks → 25 actionable tasks: 25 executed)
Task 2: complete (commits cc9ed93..cc9ed93, tests: ./gradlew testDebugUnitTest --tests *ResultFormatterTest* --tests *SicherheitsdarstellungTest* --rerun-tasks → 25 actionable tasks: 25 executed)
Task 3: Ruling: die freigegebene Mehransichten-Spec (2026-09-24) ersetzt den klickbaren Knopf "Bild analysieren" durch einen 88-dp-Haltering, und der Plan verlangt an anderer Stelle selbst "no text inside the shutter" — die sichtbare Textaktion wird deshalb NICHT gebaut; die Absicht des Review-Focus (Touch und Screenreader nennen dieselbe sichtbare Handlung) wird stattdessen über `cd_shutter` "Gedrückt halten, um Hut, Unterseite und Stiel / Ring zu erfassen" plus die sichtbare Anweisung `view_scan_hint` erfüllt und in BedienungTest/SystemBarsLayoutTest gepinnt — cost if wrong: die wörtliche Formulierung "Bild analysieren" existiert nicht in der Oberfläche, die dahinterliegende Anforderung bleibt aber geprüft.
Task 3: complete (commits 12a414d..HEAD, tests: ./gradlew testDebugUnitTest --rerun-tasks → BUILD SUCCESSFUL; ANDROID_SERIAL=emulator-5554 ./gradlew connectedDebugAndroidTest --rerun-tasks → 12/12 Tests, 0 failed)

## Fortsetzung: Segmentierer-Asset ausgeliefert und Branch gesichert (2026-09-25)

Task 4: Ruling: `ViewpointSegmenter` prueft den Kanalvertrag jetzt gegen die **Modellausgabe** (`getOutputTensor(0).shape().lastOrNull()`), nicht gegen eine eigene Konstante — `ViewpointContract.requireOutputChannels(CHANNELS)` mit festem `CHANNELS = 6` war ein Tautologie-Check, der immer 6 verglich und jeden Fremd-Export durchgelassen haette — cost if wrong: ein klasseninkompatibles Asset haette als "verfuegbar" gegolten und der Nutzer haette nie ein Ergebnis bekommen.
Task 4: BEFUND (Geraetetest): `viewpoint.tflite` gibt NHWC `[1, 300, 300, 6]` aus; `masksFor()` hat NCHW `[1, 6, 90000]` entgegengenommen → `IllegalArgumentException`, **jeder** Hold-to-scan waere abgestuerzt. Auf der JVM grundsaetzlich nicht pruefbar: die Tensorform ist eine Eigenschaft der Binaerdatei, kein Unit-Test laedt im JVM-Test ein Modell. Gefunden nur durch einen Instrumented-Test, der das **ausgelieferte** Asset wirklich laedt und einmal durchlaeuft. Fix in d2ec1c6. Details: docs/superpowers/notes/geraetetest-tensorform-2026-09-25.md
Task 4: Ruling: die Vorab-Auslieferung des Assets ist zulaessig, weil die Nutzung die kalibrierten Schwellen aus `viewpoint.json` fail-closed laedt und ohne sie jeder Schritt offen bleibt — das Asset allein erzeugt kein gruenes Urteil — cost if wrong: ein unkalibriertes Modell in der APK, aber ohne Wirkung auf die Anzeige.
Task 4: complete (commits 044b090..9d49085, tests: 152 Unit-Tests 0 failed; ANDROID_SERIAL=emulator-5554 ./gradlew connectedDebugAndroidTest --rerun-tasks → 14/14 Tests, 0 skipped, 0 failed, darunter `aShippedSegmenterIsAvailableAndEmitsEvidenceWithACompetitor`, das das ausgelieferte Asset laedt und einmal ausfuehrt)
Ruling: Branch `feat/sichere-erkennung-ui` war 7 Commits vor `main` und existierte **nur in einem Worktree auf dieser Maschine** (origin kannte nur `main`). Fast-forward-Merge und Push nachgeholt; remote `main` = 9d49085, verifiziert per `git ls-remote`. Worktree und Branch entfernt.
Ruling: das **Redmi-Runtimecheck** (App startet, Hold-to-scan laeuft ohne Absturz) belegt den Fix indirekt — die Ansichten blieben zu, weil der Testframe zu dunkel war ("Mehr Licht"), also war der Segmentierer-Pfad nicht zwingend belegt. Die belastbare Zusicherung liefert der Instrumented-Test auf dem Emulator, der `segmenter.available` prueft. Der manuelle Feldtest auf dem Redmi bleibt offen (offener Punkt aus geraetetest-tensorform-2026-09-25.md).
Hinweis: dieses Ledger ist **untracked** (kein Eintrag in .gitignore noch in .git/info/exclude, `git ls-files` kennt es nicht). Es ging beim Entfernen des Worktrees verloren und wurde aus dem Sessionverlauf rekonstruiert. Empfehlung: entweder unter `docs/superpowers/` tracken oder bewusst als Wegwerf-Artefakt behandeln.
